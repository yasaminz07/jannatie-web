rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Profile avatars — owner can read/write their own
    match /avatars/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
